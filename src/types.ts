export type TodoProps = {
	todo: {
		userId: number
		id: string
		title: string
		completed: boolean
	}
}

export type TodoProp = {
		userId: number
		id: string
		title: string
		completed: boolean
}

export type deleteTodoProp = {
	id : string 
}