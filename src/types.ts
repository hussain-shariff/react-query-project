export type TodoProps = {
	todo: {
		id ?: string
		title: string
		completed ?: boolean
	}
}

export type TodoProp = {
		id ?: string
		title : string
		completed ?: boolean
}

export type deleteTodoProp = {
	id : string 
}